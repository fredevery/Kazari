Please generate a prompt that instructs an LLM to perform the following tasks:

* Implement task management features, including task creation, completion tracking, prioritization, and session planning.
* Allow users to mark tasks as completed during planning or session transitions, and update productivity metrics accordingly.
* Support a sorted list of tasks for session planning and allow users to select the next task for each session.
* Follow the prompt structure as detailed in `.project/templates/00_PROMPT_TEMPLATE.md`. Write the prompt as Markdown and place it under the `prompts` directory as `04-task-management.md`.
